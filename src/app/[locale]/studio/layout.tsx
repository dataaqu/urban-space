import StudioPagesLayout from '@/components/studio/StudioPagesLayout';

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return <StudioPagesLayout>{children}</StudioPagesLayout>;
}
